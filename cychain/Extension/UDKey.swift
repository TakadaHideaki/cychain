struct Name {
    enum KeyName: String {
        case flag
        case uniqueNmame
        case selectCell
        case block
    }
    

    enum DataKey: String {
        case my
        case target
        case message
        case image
    }
    
}
